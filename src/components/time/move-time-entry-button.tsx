'use client'

// Re-exported hook so TimeEntryRow can import move logic cleanly.
// Actual UI is rendered inside TimeEntryRow (requires <tr>/<td> context).

export { } // module marker — see TimeEntryRow for move implementation
